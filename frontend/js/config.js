// Single edit point for the backend API URL per environment.
// Local dev default below assumes the backend runs on the same machine at port 8000.
window.FORENSIX_API_BASE = "http://127.0.0.1:8000";
